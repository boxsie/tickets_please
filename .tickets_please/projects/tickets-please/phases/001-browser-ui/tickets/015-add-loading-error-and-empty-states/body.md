Polish app states across navigation, board, drawers, forms, and search. Add skeletons, empty columns, toasts, retry controls, and consistent API error mapping so the browser UI feels reliable under normal failure modes. Acceptance criteria: network and domain errors are human-readable; loading states do not shift layout badly; empty project, phase, wave, column, and search states are useful; destructive or irreversible actions have confirmation where needed.
