Create project, phase, and wave navigation for the browser UI. The default screen should load projects, select the tickets-please project when available, list phases, and expose Browser UI waves as filters or tabs. Acceptance criteria: users can switch projects, phases, and waves; list_waves drives the wave controls; empty states are clear; selected project, phase, and wave survive reload via URL state or local state.
