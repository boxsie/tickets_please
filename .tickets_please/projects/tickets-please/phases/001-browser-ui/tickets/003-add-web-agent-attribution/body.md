Register a local web agent when tickets_please web starts and thread its session id into mutating HTTP requests via the existing service session context. Defaults should produce readable attribution such as tickets_please_web while leaving room for future config or environment overrides. Acceptance criteria: create, update, move, complete, and comment actions from the browser show correct created_by, completed_by, or author attribution; expired sessions re-register cleanly; read-only API calls do not require a session; tests verify attribution on at least one ticket mutation and one comment mutation.
