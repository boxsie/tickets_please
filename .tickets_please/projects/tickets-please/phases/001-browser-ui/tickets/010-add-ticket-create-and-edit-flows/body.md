Implement create and edit ticket dialogs with title, body, phase, wave, depends_on, and parallelizable_with inputs. Use server validation as the source of truth and show clear field-level or form-level errors. Acceptance criteria: creating a ticket places it in todo; editing title, body, or wave updates the card and detail drawer; invalid wave or title inputs show errors; dependency fields can reference existing tickets.
