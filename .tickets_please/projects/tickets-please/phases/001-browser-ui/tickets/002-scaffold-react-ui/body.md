Add a Vite React TypeScript frontend using Tailwind, shadcn/ui or Radix primitives, TanStack Query, and dnd-kit. Set up scripts for dev, build, typecheck, and test without disturbing the existing Go commands. Acceptance criteria: the frontend dev server renders the actual app shell; component system and theme tokens are established; npm scripts are documented; the production build outputs static assets ready for embedding in the Go binary.
