Embed the production frontend build into the Go binary and serve it from tickets_please web, falling back to index.html for client-side routes. Keep API routes under /api so browser traffic and JSON handlers remain clearly separated. Acceptance criteria: make build includes frontend assets when built; loading / serves the app; refreshing a deep route works; missing assets return sensible 404 responses; API routes are not swallowed by the SPA fallback.
