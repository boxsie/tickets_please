Add Playwright smoke coverage for the full browser workflow: load the board, create a ticket, move a card with a comment, complete a ticket with the modal, filter by wave, add a comment, and run a search. Acceptance criteria: smoke tests run against tickets_please web in a temp data directory or isolated fixture; tests pass in CI and locally; failures capture screenshots or traces; test setup avoids real Ollama by using fake or disabled embeddings where appropriate.
