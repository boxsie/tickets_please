Render a Trello-like board for a selected project, phase, and wave with todo, in_progress, testing, and done columns. Cards should show title, blockers, wave, phase, and lightweight metadata for quick scanning. Acceptance criteria: list_tickets populates all columns; ready and blocked states are visible; the board updates after refetch; the layout remains usable on desktop and narrow screens.
