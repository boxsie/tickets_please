When a card is dropped into done, open a completion modal instead of calling move_ticket. Collect testing_evidence, work_summary, and learnings, then call complete_ticket so the same completion rules and searchable learnings apply. Acceptance criteria: all three fields are required and at least 10 characters; successful completion moves the card to done and shows completion data; failed completion keeps the card in its prior column; learnings text is visible in the detail drawer.
