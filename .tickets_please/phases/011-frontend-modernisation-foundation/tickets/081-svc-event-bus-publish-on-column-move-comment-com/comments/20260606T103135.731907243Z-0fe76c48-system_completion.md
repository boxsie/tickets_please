---
id: 0fe76c48-d673-4609-9010-5a2a6c519566
ticket_id: c4325502-ca3c-448f-9b0c-214318da30fc
kind: system_completion
author_id: 230d1b01-f336-4764-a788-f378a0463b8e
created_at: 2026-06-06T10:31:35.731907243Z
---
✅ Ticket completed.

Testing evidence:
go build ./... clean; full go test ./... green. New svc/events_test.go uses a recordingPublisher fake wired via s.SetPublisher and exercises CreateTicket→MoveTicket→CreateComment→Archive→Unarchive→CompleteTicket on a real project, asserting exactly one event per path with correct Kind, topics (ticket:/project:), from/to columns, and actor (ByAgentID). Separate tests assert RegisterAgent emits AgentRegistered on global:agents, and that a Service with no publisher set (Nop default) mutates without panicking.

Work summary:
Added eventbus publish calls fire-and-forget after each mutation's commit + cache apply: MoveTicket→KindTicketMoved, CompleteTicket→KindTicketCompleted (ToColumn=done), CreateComment→KindCommentAdded (ticket: topic only), flipArchive→KindTicketArchived/KindTicketUnarchived, RegisterAgent→KindAgentRegistered (global:agents + agent:), touchAgentDebounced→KindAgentSeen. New internal/svc/events.go: ticketTopics(id,project,phase*) builds the fan-out set (adds phase: only when assigned), withActor(ev,agent) fills By* attribution from the agent + acting-for UserRef, derefStr for *string→string. Imports added across tickets.go/comments.go/archive.go/agents.go/middleware.go.

Learnings:
The Publisher seam was already in place from #080 (s.publisher with Nop default + SetPublisher + s.publish helper), so #081 is purely call-site wiring — no constructor signature churn, no nil-checks at call sites. Publish AFTER the cache-apply block (not just after op.Commit) so the in-memory state already reflects the mutation if any subscriber's render path reads back through the service; the events themselves carry pure data though, so it's belt-and-braces. Gotcha for tests: Seq is stamped by the real eventbus.Bus inside Publish — a recording fake publisher sees Seq=0, so don't assert seq at the svc layer (assert it in eventbus/web transport tests instead). AgentSeen piggybacks on the existing touchDebounceWindow (the LastSeenAt write is already debounced), so no separate 10s timer was needed — the event fires at most once per touch-window per agent, which satisfies the spec's debounce intent. CommentAdded is published on ticket:{id} only per the ticket spec; if the agent-activity feed (#084) needs comments on agent:/global topics, extend Topics there rather than broadening here. withActor returns the Event by value (cheap, flat struct) which keeps the call sites a single s.publish(withActor(eventbus.Event{...}, agent)) expression.
