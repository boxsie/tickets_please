---
id: 9e623f3d-b1ca-415b-aa02-3e8d5a0ef0c7
ticket_id: 3e3c942c-ee79-4a03-ad91-79c97fe4460f
kind: user
author_id: 8fe73f01-d3eb-4738-8b20-a3c224cf0cea
created_at: 2026-05-31T11:30:54.617181906Z
---
Full W2-3 (ticket 076) completion detail — the structured complete_ticket fields had to be kept minimal because the Claude Code MCP transport repeatedly dropped the work_summary/learnings fields from the envelope (known client-side bug).

COMMIT: main a1b9f30 — feat(web,auth): auth middleware + per-project role guards + CSRF reconciliation (10 files, +466/-55).

TESTING: go build ./... clean; go vet ./internal/web/... clean; `go test ./...` exits 0 (full suite). internal/web/authmw_test.go asserts the acceptance matrix via sentinel-backed guarded routes: unauthenticated → 303 /auth/login; authenticated-but-no-membership → 403; viewer GET ok / viewer POST to member route → 403; member POST ok / member to owner route → 403; owner ok; auth-disabled (no providers) → passthrough.

WORK: internal/auth/context.go (WithUser/UserFrom); internal/domain/role.go (Role.Satisfies, owner⊇member⊇viewer); svc.Service.MembershipStore wired in the constructor; store.MembershipStore.GetMembership; internal/web/authmw.go (authMiddleware hydrating tp_user→ctx, requireSlugRole/requireTicketRole + shared guardRole, redirectToLogin with HX-Redirect for htmx, csrfID); router.go restructured with authed/pub/slugRole/tktRole wrappers + per-route role levels; withCSRF + Chrome key CSRF on csrfID (user id when authed, else agent id); /sse per-stream cookie check. All conditional on a.authEnabled = len(providers) > 0.

LEARNINGS:
- make check does NOT run go test — its target is build-windows + integrity stub. A green make check only proves non-test code compiles; go build ignores _test.go, so a broken test sails through. I shipped a test calling a non-existent svc.RegisterAgentWithID (real method: RegisterAgent) and make check was green while the web test suite didn't compile. Always run `go test ./...` directly.
- Enforcement is conditional on authEnabled = len(providers) > 0. Load-bearing: existing web tests never log in and the tool's prior life is localhost-only no-auth; unconditional enforcement breaks every existing test + the dev UX. Whoever enables auth in prod also needs W2-6 (bootstrap admin) to grant the first owner, else every authenticated user 403s on every project.
- CSRF keys on csrfID(ctx) = user id when authed (stable across tp_sid agent-cookie rotation) else legacy agent id. withCSRF (handlers_projects.go) + Chrome (router.go) call a.csrfID. withCSRF is innermost so the user is in ctx. svc.SessionIDFrom kept for agent attribution (NOT removed); user↔agent bridge is W2-5.
- Middleware order: session (tp_sid) → authMiddleware (hydrate or redirect) → role guard → withCSRF → handler. guardRole reads auth.UserFrom set by authMiddleware, so the guard must sit inside it.
- requireSlugRole resolves project via svc.GetProject(slug); requireTicketRole via svc.GetTicket(id).ProjectID (/tickets/{id} has no slug). Both → MembershipStore.GetMembership → Role.Satisfies(min).
- redirectToLogin emits HX-Redirect (200) for htmx, else 303 (a 303 inside an htmx swap is swallowed).
- Test seam: freshDeps, then set a.providers + a.authEnabled=true manually (newApp computes false from the empty test config). CreateProject under WithSessionID(id from svc.RegisterAgent); UserStore.WriteUser; MembershipStore.GrantMembership; sentinel handlers (write 299) behind the real chain MINUS withCSRF to isolate the role guard; no-jar client with explicit tp_user per request.
- /sse mounted unwrapped; auth check inside handleSSE (connection is the trust boundary). Global /settings + project-create are authenticated-only — admin-gating belongs to W2-6 (known gap).</body>
