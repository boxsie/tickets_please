---
id: 2dfa7741-f7a3-4a7d-b669-0508b0509c36
ticket_id: fc82b003-5b0f-434d-a6cd-fa1c9d92e022
kind: system_completion
author_id: 497a203a-3f2c-443f-b626-c36b54122494
created_at: 2026-06-06T18:58:22.112503143Z
---
✅ Ticket completed.

Testing evidence:
go test ./... green. New svc test TestAgentCurrentWork (created-by + recent-comment paths, todo excluded, window boundary). New web tests: Detail_RendersHeaderAndActivity (header/counters/current-work/activity rows/registration anchor), Detail_Paginates (60 tickets → page 0 has Older→ but no Newer; page 1 has ←Newer), Detail_UnknownID (non-200). Live-verified on the restarted server: GET /agents/{prolific-id} → 200 with 47 "created a ticket", 4 "commented", 192 ticket cards, the Currently-working callout, and the pager; ?page=1 shows ←Newer + the registration row. gofmt clean.

Work summary:
svc.AgentCurrentWork + test (agent_activity.go/_test.go). agents/detail.go + detail.templ (Detail page, activityRow, ActivityEntry, icon/verb helpers). handleAgentDetail + GET /agents/{id} route + pagination helper. agent-detail CSS in _src/app.css (recompiled). Web handler tests incl. pagination. Committed cleanly as ca05b41.

Learnings:
/agents/{id} detail page. New detail.go (DetailProps + ActivityEntry + icon/verb helpers) + detail.templ in the agents page package, handleAgentDetail + route, plus a new svc.AgentCurrentWork method.

REUSING PARTIALS CROSS-PACKAGE: the agents page package imports the tickets page package (pgtickets) for TicketCard + CommentRow — no import cycle because tickets imports only domain/attribution/reltime/md, never agents. ActivityEntry carries *pgtickets.TicketCardProps and *pgtickets.CommentRowProps (exactly one set per kind); the handler maps svc.ActivityItem → ActivityEntry at the render boundary. For comment rows I reused the EXISTING web-package buildCommentRow + toTemplRow helpers (the same ones the ticket thread uses) so the "Claude (for Dan)" author-label + IsSystem logic stays in one place.

"CURRENTLY WORKING ON" = svc.AgentCurrentWork(ctx, agentID, now, within), a documented heuristic: in_progress tickets where CreatedBy.ID==agentID OR the agent authored a comment within `within` of `now`. Takes now+within as params (not time.Now() internally) so it's deterministic in tests — the test asserts both paths plus that a stale comment falls outside a narrow window and a todo ticket never counts. Handler passes time.Now() + 24h.

PAGINATION without an offset API: AgentActivity only takes a limit, but it materialises the FULL sorted timeline before truncating, so the handler requests (page+1)*pageSize+1 and slices [page*size : (page+1)*size]. The +1 over the boundary is how it detects "is there an older page" without a separate count call. pageSize=50. Page 0 drops the ?page= query for a clean canonical URL (agentDetailPageHref). Prev = "Newer" (toward page 0), Next = "Older". Verified live: a 52-item agent shows Older→ on page 0 and ←Newer + the registration anchor on page 2.

The registration event (svc.ActivityItem kind agent_registered, At=agent.CreatedAt) is always the OLDEST item, so it only appears on the final page — correct, and the test asserts it renders.

LIVE-PATCH HOOK: both the index last-seen cell AND the detail last-seen <dd> carry id="agent-lastseen-{id}" — the patch targets the live-ticking ticket (#084) will morph. renderAgentPatch in sse_render.go is still the #84 stub.

TEST GOTCHA (carried from the index ticket): can't reach an empty/unknown state cleanly over HTTP for the populated case — seed via deps.Service.RegisterAgent + svc.WithSessionID(ctx,id) then CreateProject/CreateTicket/MoveTicket BEFORE Mount. Unknown-agent id returns non-200 (GetAgent → ErrNotFound → classifyServiceError).
