---
id: 4060ba49-d3d1-4a56-919a-bb92da70c623
ticket_id: 926fabb0-cf17-4be3-b0ce-baa29bdf5087
kind: system_completion
author_id: 7d69742b-eb0e-4f02-8012-0a27292cb65a
created_at: 2026-06-06T17:30:24.411912358Z
---
✅ Ticket completed.

Testing evidence:
go test ./... green. New TestProjectOverview_LeadsWithPhases asserts class="phase-lead", "+ New phase", phases/new link, the phase name + its ticket render, a phase with open tickets defaults open (` open><summary class="phase-row-summary"`), and the old `<table class="data-table">` is gone. Live curl verification against 127.0.0.1:8765 confirmed all of the above + board redirect + wave focus + phases.js served. gofmt clean.

Work summary:
Extracted phases.PhaseList shared component (index.templ) + PhaseListProps/PhaseRowProps.DefaultOpen (data.go); refactored Index to use it via indexLeadPhases helper. projects DetailProps.Phases ([]*domain.Phase table) → PhaseLead phases.PhaseListProps; detail.templ leads with a .phase-lead section (h2 + "+ New phase" + PhaseList), old phases table deleted. handlers_projects.go builds the lead block via new toPhaseListProps (handlers_phases.go). CSS .phase-lead/.phase-lead-head. Test added.

Learnings:
The "reuse the index's collapsible component" instruction meant extracting a shared PhaseList templ component, because the index previously inlined its `.phase-list` loop. Once extracted, both the index and the overview lead block call PhaseList(PhaseListProps{...}) and stay byte-identical for free — and the Unphased section comes along automatically (it's part of PhaseList). Cross-package wiring works without a cycle: the projects package may import the phases package (phases imports only domain/templ/html-template, never projects or web), so DetailProps can carry a phases.PhaseListProps field directly and detail.templ can call @phases.PhaseList. "Default expanded for phases with open tickets" is per-ROW, not a global flag — added PhaseRowProps.DefaultOpen and made phaseRow open = OpenAll || row.DefaultOpen; the handler sets DefaultOpen when Dist.Todo+InProgress+Testing > 0 (active = non-done). The web-package handler already had bucketTicketsByPhaseAndWave + phaseLessTickets in hand, so toPhaseListProps just wraps toIndexProps and tags DefaultOpen — no new bucketing. Metric-grid already compacted responsively (repeat(4) → 2 → 1 at breakpoints), so "compact to one row on wide" needed zero CSS. Verified the whole W1 live by rebuilding ~/.local/bin/tickets_please + restarting the systemd user unit, then curling: /board 302→/phases, overview has class="phase-lead"/"+ New phase"/no data-table, phases page renders id="unphased" (this repo HAS phase-less tickets), phase-detail ?wave=1 renders the focus banner + #w1 anchor, /static/phases.js 200s. Reminder for next session: the restart drops the local MCP session → must register_agent again before MCP calls.
