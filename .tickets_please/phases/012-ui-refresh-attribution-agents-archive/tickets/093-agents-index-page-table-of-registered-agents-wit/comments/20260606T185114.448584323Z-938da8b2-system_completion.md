---
id: 938da8b2-9a07-413b-9805-a79799e0d8fe
ticket_id: 249ce6f7-692e-4513-bedc-225846bbaea9
kind: system_completion
author_id: dbf897e6-28f8-4ada-afdc-5dc43a9e3c53
created_at: 2026-06-06T18:51:14.448584323Z
---
✅ Ticket completed.

Testing evidence:
go test ./... green. 3 tests: RendersAllAgents (seeds 2 agents via RegisterAgent, asserts names/heading/table+filter scaffolding/agents-{id} links/model label all render), EmptyState (component-level render asserts empty copy + no table), SidebarLink. Live-verified against the restarted systemd server: GET /agents → 200, 158 agents render with name links, data-href rows, non-zero created counts (data-sort-val="4" etc.), agents.js script tag, and the sidebar .app-nav Agents link rendering active/aria-current on /agents. gofmt clean.

Work summary:
New agents page package (data.go + index.templ), handlers_agents.go (handleAgentsIndex), GET /agents route, sidebar .app-nav Agents link, static/agents.js (filter/sort/row-click), agents-page CSS in _src/app.css (recompiled), layout.templ script tag. Committed cleanly as 4e93f9f.

Learnings:
/agents index page. New page package internal/web/components/pages/agents (data.go mirror + index.templ), handler handlers_agents.go, route in router.go, sidebar app-nav entry, static/agents.js.

ROUTING: /agents is app-global (no /p/{slug} prefix), wired with `wrap` (= authed) right alongside the other top-level routes. It does NOT depend on a selected project — confirmed by the empty-sidebar render still showing the page fine.

SIDEBAR: added a new `.app-nav` block ABOVE @ProjectPicker in layout/sidebar.templ (the ticket explicitly wanted app-global nav above the picker). Reused the existing navLink/navLinkProps + linkIsActive helpers from that package, so the Agents link gets active/aria-current styling for free.

TABLE COMPONENT NOTE: the ticket hinted "reuse the ui.Table component", but ui.Table renders plain non-interactive <th>{h}</th> with no sort affordance and no per-row data attributes. The agents table needs sortable headers + data-href rows + per-cell data-sort-val, so I hand-wrote the <table class="data-table agents-table"> using the SAME .data-table CSS class ui.Table emits (visual parity) rather than fighting the component. ui.Table currently has ZERO callers in the codebase — it's a skeleton; extending it for sortability would be its own ticket.

SORT/FILTER/ROW-CLICK = progressive enhancement in static/agents.js (idempotent via dataset.agentsWired, re-wires on htmx:afterSwap). Numeric columns sort on each cell's data-sort-val (unix-nanos for times via sortStamp(), plain ints for counts); text columns on textContent. Never-seen agents get sortStamp→"0" so they sort to the bottom, matching the server-side order. Row click navigates to data-href unless the click hit an <a>/<button> (so the copy button + name link still work).

METADATA KEYS: register_agent stores model/model_version/client_name/client_version/hostname (NO "provider" key despite the ticket's "Provider/Model" header). So the Model column shows model[·model_version] with client_name as a muted subline; honest to what's actually persisted.

DEFERRED (route doesn't exist yet): acting-for renders as PLAIN TEXT, not a /u/{user} link — that route is wave-0 follow-up 1392480e. Last-seen cell carries id="agent-lastseen-{id}" as the patch target for the live-ticking ticket (#084).

TEST GOTCHA: you CANNOT test the empty-agents state over HTTP — the web session middleware auto-registers a "Web UI" agent on the first request, so ListAgents is never empty through the HTTP path. Tested the empty state by rendering agentspg.Index(IndexProps{}) directly to a strings.Builder instead. The populated-state test seeds agents via deps.Service.RegisterAgent before Mount.
