---
id: 0ca622e5-2b9f-463e-974a-3752c034a35a
ticket_id: 76f83e10-7884-409f-9beb-49b7e410d3f1
kind: system_completion
author_id: 6bb8486b-a8de-436a-8027-0ff093ce79b3
created_at: 2026-06-18T20:44:17.089639396Z
---
✅ Ticket completed.

Testing evidence:
ideation_test.go: TestPromoteIdea_FlipsInPlace (same id, kind→work, stays todo, no kind key on disk, appears in default list, original user comment + system_promote both present), TestPromoteIdea_RejectsWorkTicket (ErrFailedPrecondition), TestPromoteIdea_IntoPhase (lands in the target phase). go test ./... all 16 green; TestRegisterAllTools passes at 37 tools; go vet clean.

Learnings:
PromoteIdea (new svc/ideation.go) is flipArchive cloned: re-read ticket.yaml off disk → flip → stage yaml+comment in one StageOp → Commit under LockProject flock → enqueue comment embed → apply to cache → publish. Two key choices: (1) persist rec.Kind = KindWork.Stored() = "" so a promoted ticket is byte-identical on disk to a native work ticket (no lingering `kind: work` key). (2) Optional phase: rather than replicate the phase dir-move, PromoteIdea delegates to the existing AssignTicketToPhase AFTER the flip — but first compares the resolved target phase id to the promoted ticket's current PhaseID and skips if equal, to dodge AssignTicketToPhase's no-op-reject (ErrInvalidArgument). That means a phase-promote is TWO commits (promote + assign) — fine for hobby scale. New comment kind CommentKindSystemPromote + eventbus KindTicketPromoted (for T6 web live-patch). Tool count 36→37: updated expectedTools (Tickets 10→11, added promote_idea) AND the "all 37 names" doc comment in tools_test.go; len() is dynamic so the count check auto-tracks. SPEC.md tool count is T7's job.
