---
id: 744bbfe1-1ec4-49dd-a8ba-de4f938c8d7c
ticket_id: 2417275a-cef8-4cc6-a7a8-3bab1a69641c
kind: system_completion
author_id: 6bb8486b-a8de-436a-8027-0ff093ce79b3
created_at: 2026-06-18T20:49:07.208150255Z
---
✅ Ticket completed.

Testing evidence:
Unit: TestListTickets_OnlyIdeas (ideas-only returns idea, excludes work) + all prior ideation tests; TestRegisterAllTools green at 40 tools; go test ./... all 16 pkgs green; build+gofmt clean. Live MCP e2e (create_idea→list_ideas→list_tickets hides→promote_idea→reappears) deferred to the consolidated final verification: it needs a binary rebuild + systemd unit restart, and the web UI (T6) shares the same binary, so one rebuild validates both the ideas tools (via curl) and the web lane.

Learnings:
list_ideas/search_ideas need an "ideas-only" query that ListTicketsInput/SearchTicketsInput didn't have — IncludeIdeas only toggles inclusion, not exclusivity. Added OnlyIdeas bool to both inputs; the svc filter becomes: `if OnlyIdeas { keep iff kind==idea } else if kind==idea && !IncludeIdeas { drop }`. OnlyIdeas implies inclusion. This is the SINGLE source of truth the web ideas lane (T6) reuses — that coupling is why T5 must land before T6, not in parallel. The three tools are pure thin handlers (no new svc methods): create_idea = CreateTicket{Kind:KindIdea} with only title+body (no deps/wave/phase nagging); list_ideas = ListTickets{OnlyIdeas:true}; search_ideas = SearchTickets{OnlyIdeas:true}. Tool count 37→40: added an "Ideas (3)" group to expectedTools + bumped the "all N names" doc comment. TestFormatTicket does NOT assert an exact key set, so adding "kind" to formatTicket output didn't break it.
