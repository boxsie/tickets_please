## Testing evidence
ideation_test.go: TestListTickets_IdeaFilter (deterministic ListTickets path — default hides idea, IncludeIdeas surfaces it, work unaffected) and TestSearchTickets_IdeaFilter (async-embed path via waitForIdxLen, mirrors TestSearchTickets_IncludeArchivedFilter). go test ./... all 16 green; build + gofmt clean.

## Learnings
include_ideas mirrors include_archived exactly and they're independent && clauses (an archived idea needs both flags). Four post-filter sites, all beside the existing archived check: svc/tickets.go ListTickets; svc/search.go SearchTickets inline; and the two hydrate helpers hydrateCommentHit/hydrateLearningHit — BOTH take the flag as a new bool PARAM (not via the input struct), so you must thread it through every call site (hydrateCommentHit has TWO callers: the single-project and cross-mount comment-search paths). Tool layer: added include_ideas WithBoolean to list_tickets/search_tickets/search_learnings/search_comments registrations + the `if v,ok:=args["include_ideas"].(bool)` read in each handler. No new tools, so the 36-tool canonical-list test is untouched. search_learnings' include_ideas is effectively a no-op (ideas can't complete → no learnings) but added for symmetry.
