## Testing evidence
Markdown-only; go build + TestRegisterAllTools still green (unaffected). Cross-checked the SPEC tool list against expectedTools (40) and the new ideation tools' inline descriptions read standalone. No remaining text recommends the old "ideation phase hijacking" pattern.

## Learnings
Docs landed in three tracked-or-not places. SPEC.md (the THIRD lockstep place for tool counts — now points at expectedTools as the canonical source): bumped headline 36→40, added an "Ideas (3)" tool section + a "Ticket kinds (ideation)" narrative subsection (kind axis orthogonal to column/archived, backfill-free, lifecycle-gated, promote_idea as the one forward path), fixed pre-existing drift (Tickets section was "(8)" and never listed archive_ticket/unarchive_ticket — now "(11)" with those + promote_idea), and updated the domain-type lines (Ticket gains Kind/Archived/ArchivedAt, new TicketKind typedef, CommentKind constants now include archive/unarchive/promote) + Service API (PromoteIdea, CreateTicket kind, ListTickets IncludeIdeas/OnlyIdeas, ready_only excludes ideas). README.md: 35→40 tools / eight→nine categories, added Ideas row + promote_idea, a "What makes this different" ideation bullet, and fixed Phases 7→8 (archive_phase was missing). CLAUDE.md (this repo): GOTCHA — it's GITIGNORED per the operator's global rule (git ls-files shows 0, git check-ignore matches), so my edits there are local-only and intentionally NOT committed; added conventions for the kind axis (don't hijack tickets to ideate), the templ/css regen step, and the three-place tool lockstep. Only SPEC.md + README.md were committed.
