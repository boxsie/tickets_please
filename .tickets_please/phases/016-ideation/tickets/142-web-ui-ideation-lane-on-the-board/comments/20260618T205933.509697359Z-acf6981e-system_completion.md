---
id: acf6981e-db1f-4d85-a98e-d6931f61b7e0
ticket_id: 65cc4d50-86d6-40f3-a912-9a278091240e
kind: system_completion
author_id: 6bb8486b-a8de-436a-8027-0ff093ce79b3
created_at: 2026-06-18T20:59:33.509697359Z
---
✅ Ticket completed.

Testing evidence:
ideas_pref_test.go: TestIdeas_Overview_TogglesVisibility (default board hides idea titles + shows the toggle; ?include_ideas=true lists the idea and sets tp_show_ideas=1 cookie) and TestPromote_Web_FlipsIdea (POST /tickets/{id}/promote with csrf → kind flips to work, 303, ticket then appears on the default board). These render the real page through freshServerWithDeps — stronger than a static snapshot. templ generate + make css clean; go test ./... all 16 pkgs green; go vet ./internal/web clean.

Learnings:
Web ideation lane mirrors the archived UI surface-for-surface. Pieces: (1) ideas_pref.go = exact clone of archived_pref.go — resolveShowIdeas/setShowIdeasCookie/ideasToggleHref, cookie tp_show_ideas, query precedence ?include_ideas > cookie > false. (2) ui.IdeasToggle reuses the .archived-toggle CSS class (role=switch) so no new toggle styling needed. (3) attribution.IdeaPill(kind) is a no-op unless KindIdea — dropped next to ArchivedPill in ticket_card/detail-deps/search_results/wave_section; ticket_card needed a domain import added. (4) Dashboard lane: handler does a SEPARATE ListTickets{OnlyIdeas:true} (the work board already excludes ideas by default), threaded as Ideas/ShowIdeas/IdeasToggleHref through projectDetailData→DetailProps; lane card collapses to a "N ideas hidden" hint when the toggle's off. (5) Promote: new IdeaBadge (stable #ticket-kind span, mirrors ArchivedBadge) + PageActions idea-branch (Promote/Edit/Delete instead of Complete/Move/Reassign — those'd 422 on an idea) + dlg-promote modal (comment required + optional phase <select>) + handleTicketPromote + route POST /tickets/{id}/promote + SSE promotedPatch (re-renders IdeaBadge + PageActions so the button flips live). KindTicketPromoted also added to the phase SSE switch so a promoted phased idea appears in the wave list. GOTCHAS: after editing .templ you MUST `templ generate` (TEMPL=templ v0.3.1020 on PATH) — the _templ.go is what compiles, so a new templ component reads as "undefined" until regen. CSS: tailwindcss v4 on PATH; edit static/_src/app.css then `make css` to rebuild the minified static/app.css (added .badge-idea violet + .ideas-lane left-rail). The pages/projects package must NOT import web (props mirror pattern) — added fields to BOTH web.projectDetailData and projectspg.DetailProps + the detailToProps conversion. SCOPE: did not add a standalone include_ideas toggle to the web /search page (the dashboard lane + MCP search_ideas cover finding ideas); IdeaPill IS on search hits so a shown idea reads correctly.
