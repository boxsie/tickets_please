---
id: 3f134b38-a720-4cd3-9e5a-45735beef21a
ticket_id: 9b39533a-b49c-4b60-9201-558566a2af89
kind: system_completion
author_id: 311e9ac4-3e14-4fb2-a993-cbcfa34776b0
created_at: 2026-05-03T07:37:03.904614843Z
---
✅ Ticket completed.

Testing evidence:
Subagent commit 587e929 merged. go build clean, go vet clean, go test all PASS, race detector clean across svc mcptools cache. 9 new registry_test.go tests cover register/resolve, slug collision, idempotent re-register, LRU eviction with silent re-mount, unmounted error path, concurrent register+resolve, eager mount, non-absolute path rejection.

Work summary:
Added ProjectMount type and projectMounts map plus mountsMu in svc Service. New methods RegisterProjectMount validates path reads project.yaml checks slug-uuid collision builds Store inserts and applies LRU eviction; ResolveProjectStore re-mounts silently when evicted; WalkProjectMounts iterates registry. Wired register_agent handler to call RegisterProjectMount with slug cross-check. Refactored cache projectcache.go from store field to Resolvers closure struct (ResolveStore WalkAllStores FsnotifyEnabled); added NewWithStore convenience for single-store callers. Cache test sites updated. Eager-mount of cfg.DataDir parent in NewWithEmbed preserves stdio bootstrap unchanged.

Learnings:
Took Option A (less churn): kept Service.Store as default fallback rather than threading slug through every existing svc method. Multi-project routing happens at cache layer via per-slug resolver closures and at the new svc registry methods. Future ticket can convert per-method s.Store.X calls to ResolveProjectStore(slug).X as handlers become multi-project aware. Eviction nils the Store pointer rather than calling Close — Store has no goroutines or open handles to release. ProjectMount.Store can be nil for evicted entries so WalkProjectMounts callers must nil-check. cache.New signature changed first arg now Resolvers not Store; use NewWithStore for single-store case. Eager mount in NewWithEmbed when cfg.DataDir parent has project.yaml means stdio path needs no register_agent call to work.
